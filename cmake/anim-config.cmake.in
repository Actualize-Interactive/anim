@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/anim-targets.cmake")

# Set variables for static/shared libraries
set(ANIM_STATIC_LIBRARIES anim::anim_static)
set(ANIM_SHARED_LIBRARIES anim::anim_shared)

check_required_components(anim)
